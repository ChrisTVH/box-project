# Translation tasks

- [x] Spanish (`es`) — translate and validate `src/box/locale/es/LC_MESSAGES/box.po`; preserve placeholders, markup, shortcuts, and glossary terms.
  - [x] Translation phase (translator-es): fill the 14 new empty `msgstr` entries (installer, X11 launch, sandbox/game validation, config, runtime authenticity); neutral Spanish, glossary terms intact. — Done: 14 entries translated; `host` → `servidor`, `user-site`/`root`/`X11`/`keylogging`/`HTTP`/`HTTPS` kept in English.
  - [x] Review phase (spell-checker-es): review the filled entries for grammar, style, placeholders, and format; mark the parent entry `[x]` only after `msgattrib --untranslated` is clean and `tests/test_i18n.py` passes. — Done: 14 new confirmed (0 changed), 12 fuzzy resolved (12 corrected, 0 kept); `grep '#, fuzzy'` clean, .mo/tests skipped per scope.
  - force-reinstall strings translated, reviewed: 2 entries (`reinstall the package...` + `Reinstalling the same version ({installed})...`) confirmed neutral Spanish, `{installed}` intact, no fuzzy, consistent with neighboring entry; validation by file read only, no .mo/tests per scope.
  - micro-batch 2026-09-12: 12 empty `msgstr` reviewed (Bubblewrap/GnuPG/user-namespaces/OK/install-plan/completions/PATH warning), 0 changed, placeholders intact, glossary consistent; 3 fuzzy resolved (installed/removed {count} completions corrected, show-commands corrected, 0 kept, 0 left fuzzy); validation by file read + grep `fuzzy` clean, no .mo/tests per scope. — Reviewed.
  - micro-translation 2026-09-12: `Select an NW.js runtime 1-{count} (default 1), or [q]uit: ` translated to neutral Spanish, `{count}`/`[q]`/NW.js intact; no .mo/tests/commit per scope. — Reviewed: prompt confirmed unchanged (tú form, `{count}`/`[q]`/NW.js intact, consistent with sibling prompts); 2 fuzzy resolved (Installed runtimes corrected to installed heading, runtime-cancelled corrected to specific lowercase, 0 left fuzzy); validation by file read + rg `fuzzy` clean.
  - micro-batch 2026-09-12: 5 fuzzy resolved (1-10 selectors ×2, ten-versions page, version-list {error}, retry-page [r]/[q]), 0 left fuzzy; validation by file read + rg `fuzzy` clean, no .mo/tests/commit per scope. — Revisado.
  - micro-batch 2026-09-12: 2 fuzzy resolved (EasyRPG installed header, EasyRPG 1-{count}/[q] prompt), 0 left fuzzy; validation by file read + rg `fuzzy` clean, no .mo/tests/commit per scope. — Revisado.
  - micro-batch 2026-09-12: 2 fuzzy resolved ({sdk} NW.js-only diagnose + {sdk}/{copy_root_file} NW.js-only launch, stale {runtime} removed), 0 left fuzzy; validation by file read + grep `fuzzy` clean, no .mo/tests/commit per scope. — Revisado.
