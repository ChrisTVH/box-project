# Translation tasks

- [ ] `<language>` (`<locale>`) — translate and validate `path/to/catalog.po`; preserve placeholders, markup, shortcuts, and glossary terms.
- [x] Español (`es`) — EVB batch: `box-rpg/src/box/locale/es/LC_MESSAGES/box.po` solo entradas con `#:` en `evb_unpack.py`, `evb.py`, `paths.py` (31 sin traducir + 22 difusas resueltas); validación: `msgfmt --check -o /dev/null` OK y `msgattrib --untranslated/--only-fuzzy --only-file=/tmp/opencode/box.pot` vacíos — validado (build: `.mo` compilado, 390 mensajes, `msgattrib` vacío, spot-check OK).
- [x] Revisión Español (`es`) — EVB batch revisado: `box-rpg/src/box/locale/es/LC_MESSAGES/box.po` solo entradas con `#:` en `evb_unpack.py`, `evb.py`, `paths.py` (53 nuevas revisadas, 0 correcciones); validación: `msgfmt --check -o /dev/null` OK y `msgattrib --untranslated/--only-fuzzy --only-file=/tmp/opencode/box.pot` vacíos — validado (build: `.mo` compilado, 390 mensajes, `msgattrib` vacío, spot-check OK).
- [x] Español (`es`) — EVB source-keyed profile batch: `box-rpg/src/box/locale/es/LC_MESSAGES/box.po` sin entradas nuevas (todos los msgid reutilizados); `msgmerge --update` solo marca obsoleto `refusing unsafe unpacked game cache name`; validación: `msgfmt --check` OK y `msgattrib --untranslated/--only-fuzzy --only-file=/tmp/opencode/box-new.pot` vacíos — validado (build: `.mo` compilado, 389 mensajes, `msgattrib` vacío).
