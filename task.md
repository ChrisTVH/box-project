# Translation tasks

- [ ] `<language>` (`<locale>`) — translate and validate `path/to/catalog.po`; preserve placeholders, markup, shortcuts, and glossary terms.
- [x] es (box-rpg-maker danger-zone batch) — traducido `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (6 msgids: Danger zone, Delete launcher cache…, Remove all launcher data…, Delete all data and uninstall backend, This removes launcher cache… DELETE ALL…, Delete All Data); validación: `msgfmt --check` OK, 215 traducidos, 1 difusa heredada intacta.
- [x] es-revisión (box-rpg-maker danger-zone batch) — revisado `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (mismos 6 msgids); sin ediciones: español neutro, tuteo, género, `DELETE ALL`/`box-rpg` literales y glosario OK; validación: `msgfmt --check` OK, 215 traducidos, 1 difusa heredada intacta (salida a /tmp; `.mo` distribuido desactualizado, preexistente).
- [x] es (box-rpg uninstall batch) — traducido `box-rpg/src/box/locale/es/LC_MESSAGES/box.po` (11 msgids de `box-rpg/src/box/api/uninstall.py`: cannot resolve HOME, refusing filesystem root, cannot inspect wipe path, symlinked path, unexpected path, home directory, outside home, through symlink, owned by another user, cannot safely open, cannot remove); estilo «se rechaza…/no se puede…», `{path}` y `HOME` literales; glosario ampliado (full wipe, wipe path, filesystem root, HOME); validación: `msgfmt --check` OK, 0 difusas, 0 sin traducir (salida a /tmp).
- [x] es-corrección (box-rpg-maker uninstall + danger-zone update) — traducido `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (3 msgids: backend uninstall failed., Delete launcher cache… Cached profiles…, This removes… including cached profiles…); «Falló la desinstalación del backend.», `DELETE ALL`/`box-rpg` literales, glosario ampliado (cached profile, source save, uninstall); validación: `msgfmt --check` OK, 0 difusas, 0 sin traducir (pot en /tmp; `backend uninstall failed: {details}` nuevo en fuente, fuera de alcance).
- [x] es (box-rpg-maker backend uninstall details) — traducido `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (1 msgid: backend uninstall failed: {details} → «Falló la desinstalación del backend: {details}», `{details}` literal, sin fuzzy); validación: `msgfmt --check -o /tmp/box-rpg-maker.mo` OK, 217 traducidos, 0 difusas, 1 sin traducir preexistente (`HOME must be an absolute path`).
- [x] es (box-rpg-maker HOME absolute path) — traducido `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (1 msgid: HOME must be an absolute path → «HOME debe ser una ruta absoluta», `HOME` literal); validación: `msgfmt --check -o /tmp/box-rpg-maker.mo` OK, 218 traducidos, 0 difusas, 0 sin traducir.
- [x] es-revisión (uninstall + danger-zone + backend uninstall + HOME) — revisado `box-rpg/src/box/locale/es/LC_MESSAGES/box.po` (11 msgids uninstall + HOME must be an absolute path) y `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (Danger zone, Delete launcher cache…, Remove all launcher…, Delete all data…, This removes… DELETE ALL…, Delete All Data, backend uninstall failed., backend uninstall failed: {details}, HOME must be an absolute path); sin ediciones: español neutro, tuteo, `{path}`/`{details}` intactos, `DELETE ALL`/`box-rpg`/`HOME` literales, sin fuzzy, glosario OK sin altas nuevas; validación: `msgfmt --check` OK (432 traducidos box, 218 maker, 0 difusas, salida a /tmp).
- [x] es (box-rpg-maker language selector batch) — traducido `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (2 msgids: Language → «Idioma», Set Language → «Fijar idioma», patrón «Set Runtime» → «Fijar entorno»); glosario ampliado (language, Set Language); validación: `msgfmt --check -o /tmp/box-rpg-maker.mo` OK, 219 traducidos, 1 difusa preexistente intacta (`System default`), 0 sin traducir.
- [x] es-corrección (box-rpg-maker language selector review) — revisado `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (3 msgids: Language → «Idioma» sin cambios, Set Language → «Fijar idioma» sin cambios patrón «Set Runtime» → «Fijar entorno» OK, System default fuzzy «Icono del motor» → «Predeterminado del sistema» sin fuzzy); glosario ampliado (System default); validación: `msgfmt --check -o /tmp/box-rpg-maker.mo` OK, 220 traducidos, 0 difusas.
- [x] es (box-rpg-maker System default concise fix) — traducido `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (1 msgid: System default «Predeterminado del sistema» → «Sistema», sin fuzzy, conciso para Adw.ComboRow «Idioma: Sistema»); glosario ajustado (System default → Sistema); validación: `msgfmt --check -o /tmp/box-rpg-maker.mo` OK, 0 difusas, 0 sin traducir.
- [x] es-revisión (box-rpg-maker System default clarity review) — revisado `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (1 msgid: System default «Sistema» → «Del sistema»); español neutro conciso, se lee «Idioma: Del sistema», evita truncado en Adw.ComboRow y desambigua de «System» → «Sistema»; glosario corregido (System default → Del sistema); validación: `msgfmt --check -o /tmp/box-rpg-maker.mo` OK, 221 traducidos, 0 difusas, 0 sin traducir.
- [x] es (box listings + version-size batch) — traducido `box-rpg/src/box/locale/es/LC_MESSAGES/box.po` (5 msgids: {version} ({size}) → idéntico, listing → «listado», Select 1-{count}… con {count}, refusing listing outside cache, refusing nested listing path estilo «se rechaza gestionar…», sin fuzzy) y `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (1 msgid: {version} ({size}) → idéntico); sin tocar obsoletos (#~) ni «Removed {removed} game(s)»; glosario ampliado (listing → listado); validación: `msgfmt --check` OK (436 traducidos box, 222 maker, 0 difusas, 0 sin traducir, salida a /tmp).
- [x] es-revisión (box listings + version-size batch) — revisado `box-rpg/src/box/locale/es/LC_MESSAGES/box.po` (mismos 5 msgids) y `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (mismo 1 msgid); sin ediciones: español neutro, tuteo «Selecciona», `{version}`/`{size}`/`{count}`/`{path}` intactos, `[n]`/`[p]`/`[q]` literales, «listado» y «se rechaza gestionar…» coherentes con vecinas, glosario OK sin altas nuevas; validación: `msgfmt --check` OK (436 traducidos box, 222 maker, 0 difusas, 0 sin traducir, salida a /tmp).
- [x] es (box-rpg-maker tag verify + AppImage update + intervals batch) — traducido `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (27 msgids: 7 de verificación de etiquetas — resolve {error}/{detail} «No se pudo resolver la etiqueta…», cloned-mismatch «La etiqueta clonada… se rechaza la instalación…», verify {error}/{detail} «No se pudo verificar…», signature-failed «Falló la verificación de la firma…», unsigned «…no está firmada; se continúa…»; 11 de AppImage — Skip «Omitir», Updating «Actualizando…», Updated «Actualizado», Update AppImage «Actualizar AppImage», Update Available «Actualización de AppImage disponible», {current}→{latest} «Actualiza para obtener…», Missing «Falta la versión de actualización», Downloading «Descargando AppImage…», Replacing «Reemplazando…», updated-restarting «AppImage actualizado, reiniciando…», Update Failed «Actualización fallida»; 9 de intervalos — No «No», «Cada 12 horas», Daily «Diariamente», «Cada 2/3 días», Weekly «Semanalmente», Automatic updates «Actualizaciones automáticas», Check «Buscar actualizaciones», Set Updates «Fijar actualizaciones»); `{tag}`/`{detail}`/`{error}`/`{resolved}`/`{expected}`/`{current}`/`{latest}` y `box-rpg`/`AppImage`/`→`/`…` literales, 6 fuzzy corregidos sin fuzzy, sin tocar obsoletos (#~) ni «Removed {removed}»; glosario ampliado (tag, resolve, signature verification, AppImage, update, automatic updates, check for updates, Set Updates, skip, replace, restart, mirror); validación: `msgfmt --check -o /tmp/box-rpg-maker.mo` OK (249 traducidos), `msgattrib --untranslated/--only-fuzzy` vs /tmp POT sin salida (0/0), pytest no ejecutado (pytest no instalado).
- [x] es-revisión (box-rpg-maker tag verify + AppImage update + intervals batch) — revisado `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (mismos 27 msgids: 7 verificación etiquetas, 11 AppImage, 9 intervalos); sin ediciones: español neutro, tuteo «Actualiza», `{tag}`/`{detail}`/`{error}`/`{resolved}`/`{expected}`/`{current}`/`{latest}` intactos, `AppImage`/`→`/`…` literales, sin fuzzy, glosario OK sin altas nuevas, sin tocar obsoletos (#~) ni «Removed {removed}»; validación: `msgfmt --check -o /tmp/box-rpg-maker.mo` OK (249 traducidos), `msgattrib --untranslated/--only-fuzzy` vs /tmp POT sin salida (0/0).
- [x] es (box-rpg-maker ci-mount diagnostics + library veil + annotated tag) — traducido `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (10 msgids: 7 del grupo Diagnostics — `Diagnostics` «Diagnóstico», `Set Diagnostics` «Fijar diagnóstico», descripción de dos frases «Registra la traza de ci-mount…», `Record ci-mount debug log` «Registrar el log de depuración de ci-mount», `Applies to the next game launch.` «Se aplica al siguiente inicio del juego.», `ci-mount log file` «Archivo de log de ci-mount», `Trace file: {path}` «Archivo de traza: {path}»; par `ngettext` «Adding a game to the library…»/«Adding {count} games to the library…» → «Añadiendo un juego…»/«Añadiendo {count} juegos…»; y el hueco preexistente `Tag {tag} is not an annotated tag; refusing to install: {detail}` → «La etiqueta {tag} no es una etiqueta anotada; se rechaza la instalación: {detail}»); 2 difusas eliminados al rellenar `Diagnostics` y `Set Diagnostics`; `ci-mount`/`libfuse3`/`{path}`/`{count}`/`{tag}`/`{detail}` y la elipsis `…` (U+2026) literales; glosario ampliado (annotated tag, launch (noun), Diagnostics, Set Diagnostics, debug log, log file, trace (log), adding a game); validación: `msgfmt --check` OK (263 traducidos), `msgattrib --only-fuzzy` 0 y `--untranslated` 0, `.mo` recompilado y en sync, `pytest box-gui/tests/test_i18n_app.py` 8 passed.
- [x] es-revisión (box-rpg-maker ci-mount diagnostics + library veil + annotated tag) — revisado `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (10 entradas: `Diagnostics`, su descripción de dos frases, `Record ci-mount debug log`, `Applies to the next game launch.`, `ci-mount log file`, `Set Diagnostics`, `Trace file: {path}`, el par `ngettext` «Adding a game…/Adding {count} games to the library…» y `Tag {tag} is not an annotated tag; refusing to install: {detail}`); sin ediciones: «Añadiendo un juego a la biblioteca…» es correcto (gerundio + artículo contable es natural; los vecinos «Inspeccionando {path} …»/«Descargando AppImage {tag} …» omiten el artículo porque llevan ruta o nombre propio, y «Actualizando…» no lleva objeto, así que el patrón se aplica bien), «Fijar diagnóstico» sigue «Set Runtime/Set Language/Set Updates» → «Fijar entorno/idioma/actualizaciones» y funciona como encabezado de diálogo, «registro» frente a «log» resuelto como «Registrar el log de depuración…» (evita «Registrar el registro…»), «traza» frente a «log» refleja la inconsistencia del propio msgid inglés (`ci-mount log file` vs `Trace file`) para el mismo archivo y coincide con el glosario, «fallos de inicio de los juegos» respeta launch (noun) → «inicio» con tuteo «Déjalo…/estés», «se rechaza la instalación» coherente con la entrada vecina de etiqueta clonada; `{tag}`/`{detail}`/`{count}`/`{path}` y `…` (U+2026) intactos byte a byte, `python-brace-format` conservado, sin fuzzy, glosario OK sin altas nuevas; validación: `msgfmt --check` OK (263 traducidos), `msgattrib --only-fuzzy` 0 y `--untranslated` 0, `.mo` recompilado y en sync, `pytest box-gui/tests/test_i18n_app.py` 8 passed.
- [x] es (box-rpg-maker Debugging page title) — traducido `box-gui/src/box_gui/locale/es/LC_MESSAGES/box-rpg-maker.po` (1 msgid: `Debugging` → «Depuración», título de la nueva página de Ajustes situada antes de «Datos»); elegido «Depuración» y no «Diagnóstico» para no repetir el encabezado del grupo que la página contiene y por coherencia con la página «Depuración» de GNOME Settings y con el nombre interno `depuracion`; glosario ampliado (Debugging (page), y precisada la fila Diagnostics); `.mo` recompilado y en sync; validación: `msgfmt --check` OK (264 traducidos), `msgattrib --only-fuzzy` 0 y `--untranslated` 0, `pytest box-gui/tests/test_i18n_app.py` 8 passed.
